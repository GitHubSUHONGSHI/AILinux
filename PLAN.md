# Linux 入门学习网站 PRD

## Summary
建设一个面向零基础用户的 Linux 入门学习网站，采用 VuePress 作为文档站框架，基于 Vite + Vue3 扩展交互组件。第一版目标是让用户能按清晰路径完成 Linux 基础学习，并通过命令速查、章节练习和测验巩固知识。

默认产品形态为“文档课程站”，重点是内容结构、学习路径、可维护性和上线速度，不做账号系统、在线真实终端或复杂学习数据追踪。

## Key Changes
- 信息架构：
  - 首页：学习路径入口、核心模块导航、常用命令入口、最新章节。
  - 课程：Linux 认识、环境准备、文件系统、常用命令、权限、进程、软件包、Shell 基础、网络基础、服务管理。
  - 命令速查：按文件、文本、进程、网络、权限、压缩、包管理分类。
  - 实战练习：每章 3-5 个本地可完成任务，附参考答案。
  - 测验：章节选择题/判断题，使用 Vue3 组件在页面内完成即时反馈。
- VuePress/Vite/Vue3 实现：
  - 使用 VuePress 文档路由承载 Markdown 内容。
  - 使用 Vue3 自定义组件实现测验卡、命令示例、学习进度提示、章节导航。
  - 使用 Vite 构建能力，保持组件开发和本地预览速度。
- 内容规范：
  - 每章固定结构：学习目标、核心概念、命令示例、常见错误、练习、总结。
  - 命令示例必须说明适用场景、参数含义和安全提醒。
  - 面向初学者，避免默认假设用户已有编程或运维经验。
- 第一版不包含：
  - 用户登录、云端进度保存、真实在线 Linux Shell、付费系统、论坛社区。
  - 如需这些能力，作为后续版本规划。

## Public Interfaces / Types
- 页面路由：
  - `/` 首页
  - `/guide/` 学习路径
  - `/courses/intro/` Linux 入门
  - `/courses/filesystem/` 文件系统
  - `/courses/commands/` 常用命令
  - `/commands/` 命令速查
  - `/practice/` 实战练习
  - `/quiz/` 测验入口
- Vue3 组件：
  - `QuizBlock`：接收题目、选项、答案、解释，支持即时反馈。
  - `CommandCard`：展示命令、参数、示例、注意事项。
  - `PracticeTask`：展示任务目标、步骤提示、参考答案折叠区。
  - `LearningPath`：展示章节顺序和推荐学习进度。
- 内容数据：
  - 课程主体使用 Markdown。
  - 测验和命令速查可使用本地 JSON/YAML 数据，便于后续维护。

## Test Plan
- 内容验收：
  - 零基础用户能从首页进入学习路径，并按顺序找到全部章节。
  - 每个核心章节都有目标、示例、练习和总结。
  - 命令速查中所有命令均有示例和安全说明。
- 功能验收：
  - VuePress 本地开发服务可正常启动。
  - 所有文档路由可访问，无死链。
  - 测验组件能正确显示选项、判断答案、展示解释。
  - 命令卡和练习卡在桌面与移动端布局正常。
- 构建验收：
  - 生产构建通过。
  - 静态站点可部署到常见平台，如 Netlify、Vercel、GitHub Pages 或自有服务器。
- 可用性验收：
  - 首页首屏能明确表达“这是 Linux 入门学习站”。
  - 初学者能在 3 次点击内找到“从哪里开始学”。
  - 移动端阅读不出现文字溢出、导航遮挡或组件重叠。

## Assumptions
- 目标用户：零基础到入门阶段学习者。
- 第一版定位：可上线的 VuePress 文档课程站。
- PRD 粒度：可直接交给开发实现。
- 技术默认：VuePress + Vite + Vue3，内容以 Markdown 为主，交互组件按需嵌入。
- 默认语言：中文。
- 默认部署：静态站点部署，不依赖后端服务。
